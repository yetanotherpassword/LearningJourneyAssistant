# Generated by lja/data/synth_generator.py -- fully regenerable from the
# supplied dataset + a seed, not source data. Don't commit regenerated
# copies; see data-fixtures/README.md and python/README.md.
CSE_results_*_synthetic.xlsx

# Generated by lja/data/catalogue_generator.py from subject_catalogue.yaml --
# workbook, ground-truth sidecars and the Moodle fixture directory are all
# regenerable from the catalogue + a seed. Commit the catalogue, not these.
CSE_results_catalogue_*
moodle-generated/

# La Trobe handbook crawl (lja/data/handbook.py): page cache and the derived
# catalogues. The content is La Trobe's -- keep it out of the repo unless the
# project owner says otherwise; it regenerates from the sitemap in minutes.
handbook/
